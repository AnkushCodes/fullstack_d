name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  args: ^2.0.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  postgres: ^3.4.4
  schema_dart: ^0.0.5
  shelf_static: ^1.1.3
  shelf_proxy: ^1.0.4


